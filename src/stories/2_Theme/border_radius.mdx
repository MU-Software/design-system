import { Meta } from '@storybook/blocks'

import { defaultTheme } from '@/consts/default'

<Meta title="Theme/Border Radius" />

<div>
  # Border Radius

  {
    defaultTheme.borderRadiuses.map(
      (borderRadius, i) => <div key={i}>
        <div style={{ display: 'flex', flexWrap: 'wrap', justifyContent: 'flex-start', alignItems: 'center', gap: '1rem' }}>
          <div key={i} style={{
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            borderRadius: defaultTheme.borderRadiuses[0],
            backgroundColor: 'rgba(128, 128, 128, 0.5)',
            width: '6rem',
            height: '6rem',
            borderRadius,
          }} />
          <div>{borderRadius}</div>
        </div>
        <hr />
      </div>
    )
  }
</div>
